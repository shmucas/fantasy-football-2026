# Dependencies for the Vercel Python Function in api/index.py.
#
# Deliberately smaller than backend/pyproject.toml: this is only what serving a
# request needs. The offline pool/snapshot build pulls nflverse data with
# nflreadpy + polars (~215MB of wheels), which runs locally and would otherwise
# push the function past Vercel's 500MB bundle limit. Keep it that way - if the
# API starts needing a new import, add it here too.
fastapi>=0.115.0
httpx>=0.28.1
numpy>=2.5.1
pydantic>=2.13.4
psycopg[binary]>=3.2.0
sqlalchemy>=2.0.51
